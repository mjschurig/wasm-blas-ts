export enum Triangular {
  Upper = 'U',
  Lower = 'L',
}

export enum Transpose {
  NoTranspose = 'N',
  Transpose = 'T',
  ConjugateTranspose = 'C',
}

export enum Diagonal {
  Unit = 'U',
  NonUnit = 'N',
}

export enum Side {
  Left = 'L',
  Right = 'R',
}
